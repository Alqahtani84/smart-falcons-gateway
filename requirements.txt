# Smart Falcons Gateway — feasibility phase (Track A audit)
# Python 3.10+

# Core imaging / numerics
numpy>=1.24
Pillow>=10.0
opencv-python>=4.8
imagehash>=4.3
pillow-heif>=0.13        # HEIC/HEIF support for phone photos

# Data / tables
pandas>=2.0

# Clustering / metrics (dedup + later evaluation)
scikit-learn>=1.3
scipy>=1.10

# Progress + config
tqdm>=4.66
PyYAML>=6.0

# --- Next slice (eye detection / embeddings / POC) — not required for the audit ---
# torch>=2.1
# torchvision>=0.16
# timm>=1.0
# albumentations>=1.4
# faiss-cpu>=1.7
# matplotlib>=3.7
