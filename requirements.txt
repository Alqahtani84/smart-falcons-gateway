# Smart Falcons Gateway — feasibility phase (Track A audit)
# Python 3.10+

# Core imaging / numerics
numpy>=1.24
Pillow>=10.0
opencv-python>=4.8
imagehash>=4.3
pillow-heif>=0.13        # HEIC/HEIF support for phone photos

# Data / tables
pandas>=2.0

# Clustering / metrics (dedup + later evaluation)
scikit-learn>=1.3
scipy>=1.10

# Progress + config
tqdm>=4.66
PyYAML>=6.0

# Plots (POC report)
matplotlib>=3.7

# --- Biometric POC embeddings (real backend) ---
# CPU torch is fine for feasibility. Install torch/torchvision/timm to enable
# the DINOv2 embedder (scripts/run_poc.py --embedder dinov2). The harness runs
# without them using --embedder dummy.
torch>=2.1
torchvision>=0.16
timm>=1.0

# Optional (larger-scale search / augmentation), add when needed:
# albumentations>=1.4
# faiss-cpu>=1.7
